[package]
name = "ootleswap_pool"
version = "0.1.0"
edition = "2024"

[dependencies]
tari_template_lib = "0.26"

[dev-dependencies]
tari_template_test_tooling = "0.25"

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
opt-level = 's'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
